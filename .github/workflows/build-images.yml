name: build-images

# Builds the dashboard images and publishes them to GitHub Container Registry
# (GHCR) so they can be deployed without source (e.g. Hostinger Docker Manager
# "Compose from URL" / "Compose manually").

on:
  push:
    branches: [main]
    paths:
      - "webapp/**"
      - "db/**"
      - "pipeline/**"
      - "reconciliation/**"
      - "accounts.py"
      - "scan_*.py"
      - ".github/workflows/build-images.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: backend
            dockerfile: webapp/backend/Dockerfile
          - name: frontend
            dockerfile: webapp/frontend/Dockerfile
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/email-accountant-${{ matrix.name }}
          tags: |
            type=raw,value=latest
            type=sha,format=short

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
